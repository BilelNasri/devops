apiVersion: 1

providers:
  - name: 'default'
    orgId: 1
    folder: 'Spring Boot Dashboards'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /etc/grafana/provisioning/dashboards
